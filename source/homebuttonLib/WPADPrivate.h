/* If synprivate.h would contain all of the private declarations for the SYN
 * library, then would this file being listed in the line info imply that this
 * is the same sort for the WPAD library?
 */

/* On a separate note, I'm not sure what HBM uses that would be considered
 * "private" to WPAD; all the functions it uses have what would seem like a
 * "public" prefix ("WPAD", not "WPADi" or "__wpad") to me.
 */
